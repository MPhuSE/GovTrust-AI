{
  "name": "govtrust-ai",
  "version": "1.0.0",
  "private": true,
  "description": "GovTrust AI — Hệ thống hỗ trợ tiền kiểm hồ sơ dịch vụ công",
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "test": "turbo run test",
    "lint": "turbo run lint",
    "type-check": "turbo run type-check",
    "setup": "bash scripts/setup.sh",
    "demo": "bash scripts/demo.sh"
  },
  "devDependencies": {
    "turbo": "^2.0.0",
    "prettier": "^3.0.0",
    "typescript": "^5.4.0"
  },
  "engines": {
    "node": ">=20.0.0",
    "pnpm": ">=9.0.0"
  },
  "packageManager": "pnpm@9.0.0"
}
